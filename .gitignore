# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Claude Code
.claude/

# Build/Node (if we add tooling later)
node_modules/
dist/
build/
*.log
.env
.env.local

# Temp
*.tmp
*.bak

# Screenshots (never commit — may contain secrets)
Screenshot*.png
screenshot*.png
Screenshot*.jpg
screenshot*.jpg

# Outbox (drafts with credentials / PII — never commit)
outbox/

# Shopify CLI scaffolding (per-machine; client_id is fine, secret lives in OS keychain)
shopify-cli/.shopify/

# Cached GraphiQL endpoint URL (contains rotating local key)
.shopify-graphiql-url.txt
